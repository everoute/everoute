
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: securitypolicies.security.lynx.smartx.com
spec:
  group: security.lynx.smartx.com
  names:
    kind: SecurityPolicy
    listKind: SecurityPolicyList
    plural: securitypolicies
    singular: securitypolicy
  scope: Cluster
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            appliedToEndpointGroups:
              items:
                type: string
              type: array
            appliedToPorts:
              items:
                description: SecurityPolicyPort describes the port and protocol to
                  match in a rule.
                properties:
                  icmpCode:
                    format: int32
                    type: integer
                  icmpType:
                    format: int32
                    type: integer
                  portRange:
                    pattern: ^(\d{1,5}-\d{1,5})|()$
                    type: string
                  protocol:
                    description: The protocol (TCP, UDP, ICMP or SCTP) which traffic
                      must match.
                    enum:
                    - TCP
                    - UDP
                    - ICMP
                    - SCTP
                    type: string
                required:
                - protocol
                type: object
              type: array
            egressRules:
              items:
                properties:
                  action:
                    description: Action specifies the action to be applied on the
                      rule.
                    enum:
                    - Allow
                    - Drop
                    type: string
                  from:
                    description: If this field is empty, this rule matches all sources.
                    properties:
                      endpointGroups:
                        items:
                          type: string
                        type: array
                      ipBlocks:
                        items:
                          description: IPBlock describes a particular CIDR.
                          properties:
                            ip:
                              description: IPAddress is net ip address, can be ipv4
                                or ipv6. Format like 192.168.10.12 or fe80::488e:b1ff:fe37:5414
                              pattern: ^(((([1]?\d)?\d|2[0-4]\d|25[0-5])\.){3}(([1]?\d)?\d|2[0-4]\d|25[0-5]))|([\da-fA-F]{1,4}(\:[\da-fA-F]{1,4}){7})|(([\da-fA-F]{1,4}:){0,5}::([\da-fA-F]{1,4}:){0,5}[\da-fA-F]{1,4})$
                              type: string
                            prefixLength:
                              description: PrefixLength defines prefix length of ip
                                address. If ipv4, prefixLength must be any value between
                                0 and 32. If ipv6 prefixLength must be any value between
                                0 and 128.
                              format: int32
                              type: integer
                          required:
                          - ip
                          - prefixLength
                          type: object
                        type: array
                    type: object
                  name:
                    type: string
                  ports:
                    description: If this field is unset or empty, this rule matches
                      all ports.
                    items:
                      description: SecurityPolicyPort describes the port and protocol
                        to match in a rule.
                      properties:
                        icmpCode:
                          format: int32
                          type: integer
                        icmpType:
                          format: int32
                          type: integer
                        portRange:
                          pattern: ^(\d{1,5}-\d{1,5})|()$
                          type: string
                        protocol:
                          description: The protocol (TCP, UDP, ICMP or SCTP) which
                            traffic must match.
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - SCTP
                          type: string
                      required:
                      - protocol
                      type: object
                    type: array
                  priority:
                    format: int32
                    type: integer
                  to:
                    description: If this field is empty, this rule matches all destinations.
                    properties:
                      endpointGroups:
                        items:
                          type: string
                        type: array
                      ipBlocks:
                        items:
                          description: IPBlock describes a particular CIDR.
                          properties:
                            ip:
                              description: IPAddress is net ip address, can be ipv4
                                or ipv6. Format like 192.168.10.12 or fe80::488e:b1ff:fe37:5414
                              pattern: ^(((([1]?\d)?\d|2[0-4]\d|25[0-5])\.){3}(([1]?\d)?\d|2[0-4]\d|25[0-5]))|([\da-fA-F]{1,4}(\:[\da-fA-F]{1,4}){7})|(([\da-fA-F]{1,4}:){0,5}::([\da-fA-F]{1,4}:){0,5}[\da-fA-F]{1,4})$
                              type: string
                            prefixLength:
                              description: PrefixLength defines prefix length of ip
                                address. If ipv4, prefixLength must be any value between
                                0 and 32. If ipv6 prefixLength must be any value between
                                0 and 128.
                              format: int32
                              type: integer
                          required:
                          - ip
                          - prefixLength
                          type: object
                        type: array
                    type: object
                required:
                - action
                - name
                - priority
                type: object
              type: array
            ingressRules:
              items:
                properties:
                  action:
                    description: Action specifies the action to be applied on the
                      rule.
                    enum:
                    - Allow
                    - Drop
                    type: string
                  from:
                    description: If this field is empty, this rule matches all sources.
                    properties:
                      endpointGroups:
                        items:
                          type: string
                        type: array
                      ipBlocks:
                        items:
                          description: IPBlock describes a particular CIDR.
                          properties:
                            ip:
                              description: IPAddress is net ip address, can be ipv4
                                or ipv6. Format like 192.168.10.12 or fe80::488e:b1ff:fe37:5414
                              pattern: ^(((([1]?\d)?\d|2[0-4]\d|25[0-5])\.){3}(([1]?\d)?\d|2[0-4]\d|25[0-5]))|([\da-fA-F]{1,4}(\:[\da-fA-F]{1,4}){7})|(([\da-fA-F]{1,4}:){0,5}::([\da-fA-F]{1,4}:){0,5}[\da-fA-F]{1,4})$
                              type: string
                            prefixLength:
                              description: PrefixLength defines prefix length of ip
                                address. If ipv4, prefixLength must be any value between
                                0 and 32. If ipv6 prefixLength must be any value between
                                0 and 128.
                              format: int32
                              type: integer
                          required:
                          - ip
                          - prefixLength
                          type: object
                        type: array
                    type: object
                  name:
                    type: string
                  ports:
                    description: If this field is unset or empty, this rule matches
                      all ports.
                    items:
                      description: SecurityPolicyPort describes the port and protocol
                        to match in a rule.
                      properties:
                        icmpCode:
                          format: int32
                          type: integer
                        icmpType:
                          format: int32
                          type: integer
                        portRange:
                          pattern: ^(\d{1,5}-\d{1,5})|()$
                          type: string
                        protocol:
                          description: The protocol (TCP, UDP, ICMP or SCTP) which
                            traffic must match.
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - SCTP
                          type: string
                      required:
                      - protocol
                      type: object
                    type: array
                  priority:
                    format: int32
                    type: integer
                  to:
                    description: If this field is empty, this rule matches all destinations.
                    properties:
                      endpointGroups:
                        items:
                          type: string
                        type: array
                      ipBlocks:
                        items:
                          description: IPBlock describes a particular CIDR.
                          properties:
                            ip:
                              description: IPAddress is net ip address, can be ipv4
                                or ipv6. Format like 192.168.10.12 or fe80::488e:b1ff:fe37:5414
                              pattern: ^(((([1]?\d)?\d|2[0-4]\d|25[0-5])\.){3}(([1]?\d)?\d|2[0-4]\d|25[0-5]))|([\da-fA-F]{1,4}(\:[\da-fA-F]{1,4}){7})|(([\da-fA-F]{1,4}:){0,5}::([\da-fA-F]{1,4}:){0,5}[\da-fA-F]{1,4})$
                              type: string
                            prefixLength:
                              description: PrefixLength defines prefix length of ip
                                address. If ipv4, prefixLength must be any value between
                                0 and 32. If ipv6 prefixLength must be any value between
                                0 and 128.
                              format: int32
                              type: integer
                          required:
                          - ip
                          - prefixLength
                          type: object
                        type: array
                    type: object
                required:
                - action
                - name
                - priority
                type: object
              type: array
            priority:
              format: int32
              type: integer
            tier:
              type: string
          required:
          - appliedToEndpointGroups
          - priority
          type: object
        status:
          properties:
            currentAgentsRealized:
              description: The number of agents that have realized the SecurityPolicy.
              format: int32
              type: integer
            desiredAgentsRealized:
              description: The total number of agents that should realize the SecurityPolicy.
              format: int32
              type: integer
            observedGeneration:
              description: The generation observed by Lynx.
              format: int64
              type: integer
            phase:
              description: The phase of a SecurityPolicy is a simple, high-level summary
                of the SecurityPolicy's status.
              type: string
          required:
          - currentAgentsRealized
          - desiredAgentsRealized
          - observedGeneration
          - phase
          type: object
      required:
      - spec
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
