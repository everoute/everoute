name: ci

on:
  pull_request:
    branches:
      - '*'

  push:
    branches:
      - '*'

jobs:
  pr-check:
    runs-on: [ubuntu-20.04]
    steps:
      - name: Set up Go 1.17
        uses: actions/setup-go@v2
        with:
          go-version: ^1.17
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-
      - uses: RyanSiu1995/kubebuilder-action@v1.2.1
        with:
          version: 2.3.1
          etcdVersion: 3.5.0
      - run: echo "/usr/local/kubebuilder/bin" >> $GITHUB_PATH
      - name: setup openvswitch
        run: sudo sh -c "apt update && apt install -y openvswitch-switch && systemctl start openvswitch-switch"
      - run: sudo ln -sf `which go` `sudo which go` || true
      - name: run lynx unit test
        run: |
          sudo -E make test
          sudo -E make race-test
          sudo -E make cover-test
      - uses: codecov/codecov-action@v2
        with:
          files: ./coverage.out

  golint:
    runs-on: [ubuntu-20.04]
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - uses: actions/checkout@v2
      - uses: golangci/golangci-lint-action@v2
        with:
          version: v1.39
          only-new-issues: true
          # skip cache because of flaky behaviors
          skip-build-cache: true
          skip-pkg-cache: true

  run-deploy:
    runs-on: [ubuntu-20.04]

    steps:
      - uses: actions/checkout@v2
      - name: install k8s
        run: |
          curl -sfL https://get.k3s.io | K3S_KUBECONFIG_MODE=777 sh -
          cat /etc/rancher/k3s/k3s.yaml
          mkdir -p ~/.kube
          cp /etc/rancher/k3s/k3s.yaml ~/.kube/config
      - run: make images
      - name: import images to k3s
        run: |
          docker save --output /tmp/lynx-release.tar lynx/release
          sudo k3s ctr images import /tmp/lynx-release.tar
      - run: make deploy-test

  run-e2e:
    runs-on: [ubuntu-20.04]
    steps:
      - uses: actions/checkout@v2
      - name: install e2e environment dependency
        run: sudo sh -c "apt update && apt install -y openvswitch-switch && systemctl start openvswitch-switch"
      - name: Set up Go 1.17
        uses: actions/setup-go@v2
        with:
          go-version: ^1.17
      - name: allow ssh connect to localhost
        run: sudo -H sh -c "ssh-keygen -qN '' </dev/zero; cp ~/.ssh/id_rsa.pub ~/.ssh/authorized_keys"
      - run: sudo ln -sf `which go` `sudo which go` || true
      - name: setup e2e environment
        run: sudo -H -E bash tests/e2e/scripts/e2e-setup.sh
      - name: run e2e test cases
        run: sudo -H -E make e2e-test
